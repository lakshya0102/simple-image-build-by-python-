import numpy as np
import cv2
import matplotlib.pyplot as plt
 
img = np.zeros((100,256),dtype=np.uint8)
for i in range(img.shape[1]):
    for j in range(img.shape[0]):
        img[j,i]=i
 
cv2.imshow('lakshya',img)
cv2.waitKey(0)
cv2.destroyAllWindows()
